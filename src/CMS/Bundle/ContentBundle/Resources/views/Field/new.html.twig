{% extends "::admin/base_admin.html.twig" %}

{% block body -%}
	<div class="col-md-8 col-md-offset-2 col-xs-12">
		<div class="card">
			<div class="header">{{ 'cms.content.field'|trans }}</div>
			<div class="content">
				{{ form_start(form) }}
				{{ form_rest(form) }}
				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}